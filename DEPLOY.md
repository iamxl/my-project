# Деплой Frontend на бесплатные платформы

Есть несколько бесплатных платформ для деплоя React приложений. Все они просты в использовании и подходят для тестирования.

## 🚀 Vercel (Рекомендуется) ⭐

**Плюсы:** Самый простой, автоматический деплой из GitHub, отличная производительность

### Шаги:

1. **Подготовьте проект:**
   - Убедитесь, что код загружен в GitHub репозиторий
   - Или используйте локальный деплой

2. **Создайте аккаунт:**
   - Перейдите на [vercel.com](https://vercel.com)
   - Войдите через GitHub

3. **Деплой:**
   - Нажмите **"Add New Project"**
   - Импортируйте ваш GitHub репозиторий
   - Или перетащите папку `frontend` в Vercel
   - Настройки:
     - **Framework Preset:** Vite
     - **Root Directory:** `frontend` (если репозиторий содержит и backend)
     - **Build Command:** `npm run build`
     - **Output Directory:** `dist`
   - Добавьте переменные окружения:
     - `VITE_API_URL` = `http://localhost:3000/api` (для начала, потом замените на URL вашего backend)
     - `VITE_BOT_TOKEN` = ваш токен бота
   - Нажмите **"Deploy"**

4. **Получите URL:**
   - После деплоя получите URL типа: `https://your-project.vercel.app`
   - Обновите `.env` в backend:
     ```env
     WEB_APP_URL=https://your-project.vercel.app
     ALLOWED_ORIGINS=https://your-project.vercel.app,https://web.telegram.org
     ```

---

## 🌐 Netlify

**Плюсы:** Простой деплой, хорошая документация

### Шаги:

1. **Подготовьте проект:**
   - Убедитесь, что код в GitHub

2. **Создайте аккаунт:**
   - Перейдите на [netlify.com](https://netlify.com)
   - Войдите через GitHub

3. **Деплой:**
   - Нажмите **"Add new site"** → **"Import an existing project"**
   - Выберите репозиторий
   - Настройки:
     - **Base directory:** `frontend`
     - **Build command:** `npm run build`
     - **Publish directory:** `frontend/dist`
   - Добавьте переменные окружения в **Site settings** → **Environment variables**
   - Нажмите **"Deploy site"**

4. **Получите URL:**
   - URL будет типа: `https://your-project.netlify.app`

---

## ☁️ Cloudflare Pages

**Плюсы:** Очень быстрый CDN, бесплатный

### Шаги:

1. **Создайте аккаунт:**
   - Перейдите на [pages.cloudflare.com](https://pages.cloudflare.com)
   - Войдите через GitHub

2. **Деплой:**
   - Нажмите **"Create a project"**
   - Подключите GitHub репозиторий
   - Настройки:
     - **Framework preset:** Vite
     - **Build command:** `npm run build`
     - **Build output directory:** `dist`
     - **Root directory:** `frontend`
   - Добавьте переменные окружения
   - Нажмите **"Save and Deploy"**

---

## 📦 GitHub Pages (через GitHub Actions)

**Плюсы:** Бесплатно, интегрировано с GitHub

### Шаги:

1. **Создайте файл `.github/workflows/deploy.yml` в корне проекта:**

```yaml
name: Deploy Frontend

on:
  push:
    branches: [ main ]
    paths:
      - 'frontend/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
    
    - name: Install dependencies
      run: |
        cd frontend
        npm install
    
    - name: Build
      run: |
        cd frontend
        npm run build
      env:
        VITE_API_URL: ${{ secrets.VITE_API_URL }}
        VITE_BOT_TOKEN: ${{ secrets.VITE_BOT_TOKEN }}
    
    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./frontend/dist
```

2. **Настройте репозиторий:**
   - Settings → Pages → Source: GitHub Actions

3. **Добавьте Secrets:**
   - Settings → Secrets and variables → Actions
   - Добавьте `VITE_API_URL` и `VITE_BOT_TOKEN`

---

## 🔧 Настройка переменных окружения

После деплоя нужно обновить переменные окружения:

### В Vercel/Netlify/Cloudflare:
- Перейдите в настройки проекта
- Найдите "Environment Variables"
- Добавьте:
  - `VITE_API_URL` = URL вашего backend API
  - `VITE_BOT_TOKEN` = токен вашего бота

### Важно:
После деплоя frontend получит URL типа `https://your-app.vercel.app`. 

**Обновите backend `.env`:**
```env
WEB_APP_URL=https://your-app.vercel.app
ALLOWED_ORIGINS=https://your-app.vercel.app,https://web.telegram.org
```

**И обновите бота в BotFather:**
```
/myapps
Выберите ваше приложение
Измените URL на: https://your-app.vercel.app
```

---

## 🚀 Быстрый деплой через Vercel CLI

Если хотите задеплоить прямо из терминала:

1. **Установите Vercel CLI:**
```bash
npm i -g vercel
```

2. **Войдите:**
```bash
cd frontend
vercel login
```

3. **Деплой:**
```bash
vercel
```

4. **Следуйте инструкциям:**
   - Выберите проект
   - Добавьте переменные окружения
   - Готово!

---

## 📝 Пример полного .env для production

После деплоя frontend на Vercel:

**Backend `.env`:**
```env
BOT_TOKEN=ваш_токен
DATABASE_URL=postgresql://...
JWT_SECRET=ваш_ключ
PORT=3000
WEB_APP_URL=https://your-app.vercel.app
ALLOWED_ORIGINS=https://your-app.vercel.app,https://web.telegram.org
```

**Frontend переменные в Vercel:**
```
VITE_API_URL=https://your-backend.railway.app/api
VITE_BOT_TOKEN=ваш_токен
```

---

## ⚠️ Важные замечания

1. **HTTPS обязателен** для Telegram Mini App
2. **CORS настройки:** Убедитесь, что backend разрешает запросы с вашего frontend домена
3. **Переменные окружения:** Все переменные с префиксом `VITE_` будут доступны в коде
4. **Пересборка:** После изменения переменных окружения может потребоваться пересборка

---

## 🔄 Обновление после деплоя

После каждого изменения в коде:
- **Vercel/Netlify:** Автоматически пересоберут при push в GitHub
- **Вручную:** Запустите деплой снова через интерфейс или CLI

---

## 🆓 Лимиты бесплатных планов

- **Vercel:** 100 GB bandwidth, неограниченные запросы
- **Netlify:** 100 GB bandwidth, 300 build minutes/месяц
- **Cloudflare Pages:** Неограниченные запросы, 500 builds/месяц

Всех достаточно для тестирования и небольших проектов!

---

## 🎯 Рекомендация

Для быстрого тестирования используйте **Vercel**:
1. Самый простой деплой
2. Автоматический HTTPS
3. Отличная производительность
4. Бесплатный план более чем достаточен

